import { Component } from '@angular/core';

/**
 * The main component for rendering the page along the '/' route
 */
@Component({
  selector: 'app-cat-root',
  templateUrl: './cat-root.component.html',
})
export class CatRootComponent {}
